Minimal `ls` implementation using only these functions from the standard library:

 * `opendir`
 * `readdir`
 * `closedir`
 * `stat`
 * `lstat`
 * `getpwuid`
 * `getgrgid`
 * `getcwd`
 * `time`
 * `ctime`
 * `readlink`
 * `getopt`
 * `malloc`
 * `free`
